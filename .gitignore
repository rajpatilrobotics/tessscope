# Project-local environment and Python caches
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
.release-check/

# Editors and macOS
.DS_Store
.idea/
.vscode/

# Large downloaded datasets and model bundles
data/external/
artifacts/external/
artifacts/cache/
artifacts/runtime-runs/

# Track compact scientific evidence while keeping generated binary arrays local.
artifacts/runs/**
!artifacts/runs/**/
!artifacts/runs/**/*.json
!artifacts/runs/**/*.csv
!artifacts/runs/**/*.png
# The judge demo's deterministic validation replay is intentionally compact (about 13 MB).
!artifacts/runs/demo/validation-replay.npz

# Local secrets
.env
.env.local
!.env.example
